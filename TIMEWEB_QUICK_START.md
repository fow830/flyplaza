# 🚀 Быстрый старт на Timeweb.cloud

## 📋 Что нужно сделать

### 1️⃣ Создайте VPS на Timeweb.cloud

1. Перейдите на https://timeweb.cloud
2. Войдите в панель управления
3. Облачные серверы → Создать сервер
4. Выберите:
   - **OS**: Ubuntu 22.04 LTS
   - **Тариф**: Cloud Mini (1GB RAM, 1 vCPU) или выше
   - **Регион**: ближайший к вам
5. Сохраните **IP адрес** сервера

### 2️⃣ Подключитесь к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
```

Введите пароль, который вы получили от Timeweb.

### 3️⃣ Установите Docker (один раз)

```bash
curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh
apt install docker-compose -y
```

### 4️⃣ Клонируйте проект

```bash
mkdir -p /var/www && cd /var/www
git clone https://github.com/fow830/flyplaza.git
cd flyplaza
git checkout main
```

### 5️⃣ Настройте API токен

```bash
cp .env.example .env
nano .env
```

Замените `your_aviasales_token_here` на ваш настоящий токен от Travelpayouts.

**Как получить токен:**
1. Зарегистрируйтесь на https://www.travelpayouts.com
2. Перейдите в API → Aviasales
3. Скопируйте ваш токен

Сохраните файл: **Ctrl+X → Y → Enter**

### 6️⃣ Запустите приложение

```bash
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- ✅ Соберёт Docker образ
- ✅ Запустит контейнер
- ✅ Проверит работоспособность

### 7️⃣ Проверьте работу

Откройте в браузере:
```
http://ВАШ_IP_АДРЕС:3002
```

Вы должны увидеть FlyPlaza! 🎉

---

## 🌐 Настройка домена (опционально)

### Установите Nginx

```bash
apt install nginx -y
```

### Создайте конфигурацию

```bash
nano /etc/nginx/sites-available/flyplaza
```

Вставьте:

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Активируйте конфигурацию

```bash
ln -s /etc/nginx/sites-available/flyplaza /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### Добавьте A-запись в DNS

В панели Timeweb.cloud:
- Домены → Добавить домен
- DNS → Добавить A-запись
- Имя: `@`, Значение: `IP_вашего_VPS`

### Установите SSL (HTTPS)

```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

Теперь ваш сайт доступен по HTTPS! 🔒

---

## 📊 Полезные команды

### Просмотр логов
```bash
cd /var/www/flyplaza
docker-compose logs -f
```

### Перезапуск
```bash
docker-compose restart
```

### Остановка
```bash
docker-compose down
```

### Обновление кода
```bash
cd /var/www/flyplaza
git pull origin main
./deploy.sh
```

### Проверка статуса
```bash
docker-compose ps
```

---

## 🆘 Помощь

### Приложение не запускается?

1. Проверьте логи:
   ```bash
   docker-compose logs
   ```

2. Проверьте, что порт 3002 свободен:
   ```bash
   netstat -tulpn | grep 3002
   ```

3. Проверьте файл .env:
   ```bash
   cat .env
   ```

### Не работает поиск билетов?

Проверьте API токен:
```bash
cat .env | grep AVIASALES_API_TOKEN
```

Токен должен начинаться с правильного значения, а не `your_aviasales_token_here`.

### Не открывается по IP?

Проверьте firewall:
```bash
apt install ufw -y
ufw allow 3002/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

---

## ✅ Checklist

- [ ] VPS создан
- [ ] Подключился по SSH
- [ ] Docker установлен
- [ ] Проект склонирован
- [ ] API токен настроен в .env
- [ ] Приложение запущено (./deploy.sh)
- [ ] Сайт открывается по IP:3002
- [ ] (Опционально) Домен настроен
- [ ] (Опционально) SSL установлен

---

## 🎉 Готово!

Ваш FlyPlaza теперь работает на Timeweb.cloud!

📍 **Демо**: http://ВАШ_IP:3002  
📖 **Полная документация**: [DEPLOYMENT.md](./DEPLOYMENT.md)  
🐙 **GitHub**: https://github.com/fow830/flyplaza

---

**Вопросы?** Откройте Issue на GitHub: https://github.com/fow830/flyplaza/issues

