# FlyPlaza

Веб-приложение для поиска самых дешевых авиабилетов на маршруте Санкт-Петербург - Стамбул через Aviasales.ru.

## Функционал

- 🔍 Автоматический поиск билетов на 30 дней вперед
- 📅 Проверка всех дат в диапазоне
- 💰 Поиск 5 самых дешевых билетов
- 🎫 Отображение детальной информации о каждом билете
- 🔗 Прямые ссылки на Aviasales для покупки

## Начало работы

### Установка зависимостей

```bash
npm install
```

### Настройка API токена

Для работы с Aviasales API необходим токен от Travelpayouts. Получите его на [travelpayouts.com](https://www.travelpayouts.com/developers/api).

Создайте файл `.env.local` в корне проекта:

```bash
AVIASALES_API_TOKEN=ваш_токен_здесь
```

Или используйте переменную окружения:

```bash
export AVIASALES_API_TOKEN=ваш_токен_здесь
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3002](http://localhost:3002) в браузере.

### Сборка для продакшена

```bash
npm run build
npm start
```

## Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React 18** - Библиотека для создания пользовательских интерфейсов
- **Axios** - HTTP клиент для запросов
- **Cheerio** - Серверный парсинг HTML

## Структура проекта

```
flyplaza/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── flights/
│   │   │       └── search/     # API endpoint для поиска билетов
│   │   ├── layout.tsx           # Корневой layout
│   │   ├── page.tsx            # Главная страница
│   │   ├── globals.css         # Глобальные стили
│   │   └── not-found.tsx       # Страница 404
│   ├── components/
│   │   ├── FlightCard.tsx      # Карточка билета
│   │   └── FlightSearch.tsx    # Компонент поиска
│   └── lib/
│       └── flights.ts          # Утилиты для работы с билетами
├── public/                     # Статические файлы
└── package.json               # Зависимости проекта
```

## Как это работает

1. При нажатии кнопки "Найти самые дешевые билеты" запускается поиск
2. Система генерирует запросы к API для каждой даты в диапазоне 30 дней
3. Для каждой даты выполняется запрос к Aviasales API через Travelpayouts
4. API возвращает данные о билетах в реальном времени
5. Все найденные билеты фильтруются (только прямые рейсы) и сортируются по цене
6. Отображаются 5 самых дешевых билетов

## Параметры поиска

- **Маршрут:** Санкт-Петербург (LED) → Стамбул (IST)
- **Направление:** В одну сторону
- **Пассажиры:** 1
- **Период:** Текущая дата + 30 дней

## Важные замечания

⚠️ **API Ограничения:** Aviasales API имеет ограничение 100 запросов в час с одного IP. При поиске на 30 дней это означает примерно 3 запроса в час на дату.

⚠️ **Время обработки:** API обрабатывает запросы в реальном времени и может занимать 30-60 секунд на каждый запрос. Поиск на 30 дней может занять значительное время.

⚠️ **Токен API:** Необходим действующий токен от Travelpayouts. Получите его в личном кабинете на travelpayouts.com.

## Разработка

### Добавление новых маршрутов

Для добавления новых маршрутов измените параметры в компоненте `FlightSearch.tsx` или добавьте форму выбора городов.

### Обновление селекторов парсинга

Если структура HTML на Aviasales изменилась, обновите селекторы в функции `parseAviasalesPage` в файле `src/app/api/flights/search/route.ts`.

## Лицензия

Этот проект создан для образовательных целей.
